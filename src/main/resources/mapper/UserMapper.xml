<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.userapi.infrastructure.persistence.mapper.UserMapper">

    <!-- ResultMap定義 -->
    <resultMap id="userResultMap" type="com.example.userapi.infrastructure.persistence.entity.UserEntity">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- INSERT: ユーザー挿入 -->
    <insert id="insert" parameterType="com.example.userapi.infrastructure.persistence.entity.UserEntity"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO users (name, email, password, created_at, updated_at)
        VALUES (#{name}, #{email}, #{password}, NOW(), NOW())
    </insert>

    <!-- SELECT: IDで検索 -->
    <select id="selectById" resultMap="userResultMap">
        SELECT id, name, email, password, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

    <!-- SELECT: メールアドレスで検索 -->
    <select id="selectByEmail" resultMap="userResultMap">
        SELECT id, name, email, password, created_at, updated_at
        FROM users
        WHERE email = #{email}
    </select>

    <!-- SELECT: 全件取得 -->
    <select id="selectAll" resultMap="userResultMap">
        SELECT id, name, email, password, created_at, updated_at
        FROM users
        ORDER BY id
    </select>

    <!-- UPDATE: ユーザー更新 -->
    <update id="update" parameterType="com.example.userapi.infrastructure.persistence.entity.UserEntity">
        UPDATE users
        SET name = #{name},
            email = #{email},
            password = #{password},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- DELETE: ユーザー削除 -->
    <delete id="deleteById">
        DELETE FROM users
        WHERE id = #{id}
    </delete>

    <!-- COUNT: メールアドレス存在チェック -->
    <select id="countByEmail" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE email = #{email}
    </select>

</mapper>